// Sandbox connectivity smoke. Phase 0 preserved.

import { describe, test } from 'vitest';

describe('mcp-ping integration', () => {
  test.todo('connects to the sandbox via marker scan');
  test.todo('reports sandbox = ok when marker SO is present');
  test.todo('skips with a clear message when no sandbox LS is reachable');
});
